<form method="post" action="{{ path('app_pessoa_delete', {'id': pessoa.id}) }}" onsubmit="return confirm('Tem certeza que quer excluir essa pessoa?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pessoa.id) }}">
    <button class="btn btn-danger">Excluir</button>
</form>
