{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-grid gap-2 col-6 mx-auto">
        <button class="btn btn-success">{{ button_label|default('Cadastrar') }}</button>
    </div>
{{ form_end(form) }}
